{
  "name": "TypeScript + React project (happy path)",
  "skills": ["sync-rules"],
  "query": "Generate rules for this project",
  "description": "Standard TypeScript + React project with Prettier. Tests the primary workflow end-to-end.",
  "files": [
    "package.json",
    "tsconfig.json",
    ".prettierrc",
    "src/App.tsx",
    "src/components/Button.tsx",
    "src/api/users.ts",
    "src/hooks/useAuth.ts",
    "src/__tests__/App.test.tsx",
    "src/api/__tests__/users.test.ts",
    ".claude/CLAUDE.md"
  ],
  "expected_classification": {
    "source_files": ["src/App.tsx", "src/components/Button.tsx", "src/api/users.ts", "src/hooks/useAuth.ts"],
    "test_files": ["src/__tests__/App.test.tsx", "src/api/__tests__/users.test.ts"],
    "config_files": [".prettierrc", "package.json", "tsconfig.json"],
    "test_patterns": ["*.test.ts", "*.test.tsx"]
  },
  "verify": {
    "required_files": ["code-style-typescript.md"],
    "file_checks": {
      "code-style-typescript.md": {
        "has_frontmatter": true,
        "content_contains": ["Defer to Prettier"]
      },
      "testing.md": {
        "has_frontmatter": true,
        "frontmatter_contains": [".test."]
      },
      "api-design.md": {
        "has_frontmatter": true,
        "frontmatter_contains": ["src/api"],
        "frontmatter_not_contains": [".tsx"]
      }
    }
  },
  "expected_behavior": [
    "Runs tiered analysis: parallel Glob calls -> classify files (source/test/config) -> config read -> targeted grep",
    "Detects TypeScript + React as primary language/framework from package.json dependencies",
    "Detects Prettier from .prettierrc and defers formatting rules to it",
    "Generates code-style-typescript.md with paths frontmatter and 'Defer to Prettier' for formatting rules",
    "Generates testing.md with paths scoped to '\"src/**/*.test.{ts,tsx}\"'",
    "Generates api-design.md with paths scoped to API directories only (src/api/**/*.ts), excluding .tsx",
    "Presents generation plan to user and waits for confirmation before writing any files",
    "All generated files include correct frontmatter (where applicable), metadata comment, and section markers",
    "Runs validate_rules.py on generated files and all pass",
    "Reads .claude/CLAUDE.md and detects contradiction: 'No test framework' vs vitest in devDependencies",
    "Detects overlap: coding style rules in CLAUDE.md overlap with code-style-typescript.md candidate",
    "Presents CLAUDE.md Warnings section in generation plan before user confirmation"
  ]
}
